# libsha1 and libsha256

Simple implementation of the `sha1` and `sha256` hashing algorithms

## Requirements
- cmake

## Building
Creates a 2 static libraries `libsha1` and `libsha256` in the build directory.
```bash
git clone https://github.com/Phandal/libsha
cd libsha
cmake -B build -S .
cmake --build build
```
